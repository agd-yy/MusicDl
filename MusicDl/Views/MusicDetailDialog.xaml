<ui:FluentWindow
    x:Class="MusicDl.Views.MusicDetailDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cnvts="clr-namespace:MusicDl.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MusicDl.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:MusicDl.Models"
    x:Name="Dialog"
    Width="500"
    Height="600"
    d:DataContext="{d:DesignInstance Type=vm:MusicDetail}"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <ui:FluentWindow.Resources>
        <BitmapImage x:Key="DefaultCoverImage" UriSource="/Resources/CoverImage.png" />
        <Style BasedOn="{StaticResource {x:Type ui:Button}}" TargetType="ui:Button">
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,8,0,4" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="FontSize" Value="16" />
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ui:TitleBar Title="Music Detail" IsHitTestVisible="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="{StaticResource DefaultCoverImage}" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Header with Cover and Basic Info  -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  Cover Image  -->
                    <Border
                        Width="110"
                        Height="110"
                        ClipToBounds="True"
                        CornerRadius="4">
                        <Image Stretch="UniformToFill">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Source" Value="{Binding CoverUrl}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CoverUrl}" Value="{x:Null}">
                                            <Setter Property="Source" Value="{StaticResource DefaultCoverImage}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CoverUrl}" Value="">
                                            <Setter Property="Source" Value="{StaticResource DefaultCoverImage}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Border>

                    <!--  Basic Info  -->
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="{Binding Name}"
                            TextWrapping="Wrap" />

                        <!--  Duration  -->
                        <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                            <ui:SymbolIcon VerticalAlignment="Center" Symbol="Clock24" />
                            <TextBlock
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Text="{Binding Duration}" />
                        </StackPanel>

                        <!--  Quality & Size  -->
                        <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                            <Border
                                Padding="6,2"
                                Background="Black"
                                CornerRadius="4">
                                <TextBlock Foreground="White" Text="{Binding AudioQuality, Converter={cnvts:AudioQualityConverter}}" />
                            </Border>
                            <TextBlock
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Text="{Binding Size, Converter={cnvts:FileSizeConverter}}" />

                            <!--  VIP Badge  -->
                            <Border
                                Margin="8,0,0,0"
                                Padding="4,1"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="2"
                                Visibility="{Binding IsMember, Converter={cnvts:BooleanToVisibilityConverter}}">
                                <TextBlock
                                    FontSize="11"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource SystemAccentColorDark1Brush}"
                                    Text="VIP" />
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!--  Artists Section  -->
                <StackPanel Grid.Row="1" Margin="0,16,0,0">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="Artists" />
                    <ItemsControl Margin="0,4,0,0" ItemsSource="{Binding Artist}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ui:SymbolIcon VerticalAlignment="Center" Symbol="Person24" />
                                    <TextBlock
                                        Grid.Column="1"
                                        Margin="8,0,0,0"
                                        FontSize="14"
                                        Text="{Binding Name}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!--  Album Section  -->
                <StackPanel Grid.Row="2" Margin="0,16,0,0">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="Album" />
                    <Grid Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon VerticalAlignment="Center" Symbol="Album24" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            FontSize="14"
                            Text="{Binding Album.Name}" />
                    </Grid>
                    <Grid Margin="0,4,0,0" Visibility="{Binding Album.PublishTime, Converter={cnvts:StringToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon VerticalAlignment="Center" Symbol="CalendarLtr24" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            FontSize="14"
                            Text="{Binding Album.PublishTime}" />
                    </Grid>
                </StackPanel>

                <!--  URL Section  -->
                <StackPanel
                    Grid.Row="3"
                    Margin="0,16,0,0"
                    Visibility="{Binding Url, Converter={cnvts:StringToVisibilityConverter}}">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="Music URL" />
                    <ui:TextBox
                        Margin="0,4,0,0"
                        IsReadOnly="True"
                        Text="{Binding Url, Mode=OneWay}"
                        TextWrapping="Wrap" />
                </StackPanel>

                <!--  Lyrics Section  -->
                <StackPanel
                    Grid.Row="4"
                    Margin="0,16,0,0"
                    Visibility="{Binding Lyric, Converter={cnvts:StringToVisibilityConverter}}">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="Lyrics" />
                    <Border
                        Margin="0,4,0,0"
                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="4">
                        <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                            <TextBlock
                                Padding="8"
                                FontSize="12"
                                Text="{Binding Lyric}"
                                TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!--  下载按钮区域  -->
        <Grid Grid.Row="2" Margin="16,8,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button
                Grid.Column="1"
                Padding="16,8"
                Appearance="Primary"
                Click="DownloadButton_Click"
                Content="Download"
                Icon="{ui:SymbolIcon Symbol=ArrowDownload24}" />
        </Grid>
    </Grid>
</ui:FluentWindow>